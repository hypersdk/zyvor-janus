cluster:
  nodes:
    - id: node-0
      gpus:
        - id: gpu-0
          profile: H100_80GB
        - id: gpu-1
          profile: H100_80GB

scheduler:
  type: preemptive

workload:
  path: ../workloads/dual_gpu_preempt_migrate.yaml

hardware_profiles_dir: ../hardware
